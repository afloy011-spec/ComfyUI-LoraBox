[project]
name = "timur-lora-box"
description = "Timur Lora Box — compact multi-LoRA loader with a DOM-panel UI (search, per-row strength, drag reorder, trigger-word lookup)."
version = "1.1.0"
license = { text = "MIT" }
dependencies = []

[tool.comfy]
PublisherId = "timur"
DisplayName = "Timur Lora Box"
