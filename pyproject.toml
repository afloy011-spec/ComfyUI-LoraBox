[project]
name = "afloy-lora-box"
description = "Afloy Lora Box — compact multi-LoRA loader with a DOM-panel UI (search, per-row strength, drag reorder, trigger-word lookup)."
version = "1.6.5"
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
authors = [{ name = "Afloy", email = "afloy011@gmail.com" }]
keywords = ["comfyui", "lora", "loader", "z-image", "flux", "sdxl"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]

[project.urls]
Repository = "https://github.com/afloy011-spec/ComfyUI-LoraBox"

[tool.comfy]
PublisherId = "afloy"
DisplayName = "Afloy Lora Box"
Repository = "https://github.com/afloy011-spec/ComfyUI-LoraBox"
